School Class Form
<br>

<%= form_for(@class) do |c|%>
<%= c.label :title %>
<%= c.text_field :title %>
<%= c.label :room_number %>
<%= c.text_field :room_number %>
<%= c.submit  "Create School class"%>
<%end%>
